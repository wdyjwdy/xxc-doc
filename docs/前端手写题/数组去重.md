## Set
```js
function removeDuplicate(arr) {
	return [...new Set(arr)]
}
```
## 元素是否出现过
```js
function removeDuplicate(arr) {
	return arr.filter((value, index, array) => {
		return index === array.indexOf(value)
	})
}
```